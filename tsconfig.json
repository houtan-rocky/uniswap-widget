{
  "comment": "Solution file: editors resolve each package via its own tsconfig. Typecheck runs per-package (`pnpm typecheck`).",
  "files": [],
  "references": [
    { "path": "./packages/react-uniswap" },
    { "path": "./examples/basic" }
  ]
}
